apiVersion: v1
kind: Service
metadata:
  name: node-simple
spec:
  selector:
    app.kubernetes.io/name: node-source
    role: node-source
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 3500